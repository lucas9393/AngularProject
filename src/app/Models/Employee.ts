export class Employee {
    constructor(public id: number, public departmentId: number, public name: string, public surname: string, public dateOfBirth: Date, public email: string, public address: string){};
}
